# Decision Log — human-judgment moments

Required by the brief: interaction traces must show "the moments where human judgment intervened."
Every human intervention — triage selections, lead kills, editorial calls, legal-flag decisions,
scope changes — gets a row. Referenced from README §3 and from each finding's verification block.

**Fresh start (2026-07-06, second attempt):** reset a second time alongside LEDGER.md. The first
full-corpus attempt (archived at `archive/DECISIONS_full-sweep-rediscovery_2026-07-06.md`) mostly
re-derived findings already on file from the pilot pass, because it ran the same canned lens file
that originally produced them. The pilot-era decision history is separately archived at
`archive/DECISIONS_pilot-triage_2026-07-06.md`.

| date | decision | rationale | who | trace file |
|---|---|---|---|---|
| 2026-07-06 | Generated leads only from self-designed rate-of-change / emergence / contribution-flow lenses (new file `queries/emergence_and_flows.sql`); did not run any `sweep_2026.sql` block | The prior full-corpus attempt replayed the canned sweep and re-derived known anomalies; independent generation requires querying dimensions the sweep doesn't cover (trends over 2022-2026, individuals-as-clients, contribution flows, single-quarter spikes) | orchestrator | 2026-07-06_generation_emergence-flows.jsonl |
| 2026-07-06 | Set aside the mechanically-dominant top of each lens (Korea Zinc atop E1; Trump-Vance inaugural donors and the IBEW "N/A" placeholder atop F1) and promoted the mid-tier instead | Per the brief's "dig past the top-of-list" instruction: the single biggest number in a simple ranking is a surface artifact, not a finding. Korea Zinc is a known takeover fight; inaugural giving is a widely-reported category | orchestrator | 2026-07-06_generation_emergence-flows.jsonl |
| 2026-07-06 | Killed the original E2 design (a global "firms-per-client" fan-out ranking) and replaced it with an honest per-client roster drill | Client identity fragments across registrants — Senate `client_id` is registrant-scoped, and both the entity resolver and name-normalization split Vantive and TP-Link into multiple keys. A global GROUP BY would have produced clean-looking but wrong firm counts (this repo's documented failure mode). The defensible firm-count is read from the roster per client | orchestrator | 2026-07-06_generation_emergence-flows.jsonl |
| 2026-07-06 | Flagged L021 (Daibes clemency lobbying) and L023 (Vantive → White House Ballroom Project) for an editorial/legal-sensitivity review before any finding is locked | Both name living individuals and touch an active clemency ask / a project associated with the sitting President; consistent with treating legal-flag calls as a human (Rob) decision as on L003. The underlying records are public, so the leads are logged, but publication framing needs the flag cleared | orchestrator (flag pending, Rob) | 2026-07-06_generation_emergence-flows.jsonl |
| 2026-07-06 | Logged the raw corpus being absent on this box (only `db/lda_full.duckdb` present) as a verification constraint, not a blocker | Leads carry stable DB-resolved citation keys (`filing_uuid` / `filing_id` / `src_file:src_line`), which are the sanctioned identifiers; the raw-record round-trip via `show_record.py` is deferred until `data/` is re-materialised for lock-time re-derivation | orchestrator | 2026-07-06_generation_emergence-flows.jsonl |
| 2026-07-06 | Correlated ISSUE-CODE SHARES (each code's fraction of the quarter's press / spend total), NOT raw counts, for all lobbying–messaging coupling claims | The press corpus ~quadruples 2022→2025 (~5.3k→~24k releases/qtr) and spend drifts up, so raw-count correlation is ~0.8+ for almost any code purely from corpus growth — a clean-looking artifact of the exact kind CLAUDE.md warns about. Shares cancel the common trend and isolate real co-movement. Raw counts kept only as a vocabulary sanity check (P0) | orchestrator | 2026-07-06_press-issue-coupling_L026-L027.jsonl |
| 2026-07-06 | Promoted the MMM (Medicare/Medicaid) negative coupling as the primary lead (L026) and parked the TRD (tariffs) positive coupling (L027) as cold | Per the "dig past the mechanically-dominant top" discipline: TRD +0.88 is the tightest coupling but is the widely-reported 2025 Trump-tariff cycle (the obvious result); the MMM −0.72 divergence — press attention doubling while lobbying-money share falls — is the non-obvious, novel say-vs-pay story. TRD still logged so the "citable coupling" deliverable is evidenced | orchestrator | 2026-07-06_press-issue-coupling_L026-L027.jsonl |
| 2026-07-06 | Judged L026/L027 to need NO legal-sensitivity flag (unlike L021/L023) | The named actors are sitting members of Congress quoted from their own official public press releases on public legislation — routine political attribution, not the living-private-individual / active-clemency category that triggered the Rob flag on L021 and L023. Underlying records are public and the framing is ordinary say-vs-pay reporting | orchestrator | 2026-07-06_press-issue-coupling_L026-L027.jsonl |
| 2026-07-06 | Re-derived the MMM money-share decline with the `filing_period` dedup before citing it, rather than trusting `v_spend_by_issue_quarter` | The view attributes filing income to every activity code and does not dedup amendments (documented level-inflation). The lead's magnitude claim (share falling 9.09%→7.55%) had to survive the correct dedup; it did, and agreed in direction with the view, so the divergence is not a dedup artifact | orchestrator | 2026-07-06_press-issue-coupling_L026-L027.jsonl |
| 2026-07-07 | Built the bill cross-check (P2) to match a bill by BOTH a curated name-alias phrase set AND its H.R./S. number, and made the alias table carry names-vs-phrases separately with a documented Congress-ambiguity rule | The exact gap that killed L004: press/members name bills ("Farm Bill", "Inflation Reduction Act"), filings cite numbers, so number-only matching fabricates "lobbied but publicly silent" bills. Precision demanded three curation calls — ambiguous acronyms (IRA, bare "PACT Act") kept out of match phrases; low reused numbers (H.R.1/H.R.2) left phrase-primary because `bill_mentions` has no Congress field; the demo bill validated both ways (IRA: 53 number-cited press vs 3,157 name-only) | orchestrator | 2026-07-07_bill-crosscheck_P2.jsonl |
| 2026-07-07 | Logged SECURE 2.0 (L028) as an `open` candidate, not a triaged lead, and flagged that its clean signal is the free-text name match rather than the omnibus number | This session's lane was tooling; L028 is a tool-surfaced starting point a finding-owner triages. The number vehicle (H.R.2617) is the broad 2023 appropriations omnibus, so a number match over-counts — the defensible "who quietly lobbied SECURE 2.0" signal is the 1,436 senate / 1,416 house filings that name it in their own activity text | orchestrator | 2026-07-07_bill-crosscheck_P2.jsonl |
| 2026-07-07 | Ran the outside-context-scan on L028 and parked it as a known story; corrected the framing so "near-silent" is scoped to congressional member press releases, not news salience | Anchored to the Dec-2022 enactment window. Landscape check: SECURE 2.0 was high news-salience (CNBC + heavy trade/law-firm coverage). Novelty check: the retirement/annuity-industry-lobbying angle was contemporaneously reported (InvestmentNews "annuities top IRI's lobbying agenda", Retirement Income Journal, Cato). So the say-vs-pay signal is a valid P2 tool demo but not a novel lead. The "quietly lobbied / press near-silent" wording risked implying secrecy and was rewritten. Adjacent finding (logged, not pursued here): Athene's separate 2024-25 pension-risk-transfer ERISA class actions | orchestrator (scan) | 2026-07-07_bill-crosscheck_P2.jsonl |
| 2026-07-07 | Re-ran the L028 scan under the outside-context-scan skill's new contemporaneous mode (added by another session, commit 53b8fb1) to test whether the stricter method changes the call | My first pass used live/keyword WebSearch with the year in the query string — a soft filter the updated skill correctly flags as pulling later retrospectives, not publish-date-filtered contemporaneous coverage. The prescribed GDELT DOC 2.0 fetch (timelinevol + date-gated artlist) gave a rigorous answer: near-zero 2021 news volume, ~50x spike at Dec-2022 enactment, annuity angle covered contemporaneously (Forbes/MarketWatch/NBC Oct-Dec 2022, IRI named). Same park-as-known-story conclusion, now defensible, plus a nuance my first pass missed — news salience peaks at enactment (Dec 2022) while internal member-press salience peaks at House passage (Mar 2022). Operational note: GDELT via WebFetch 429'd (shared-IP rate limit); a direct curl to the same JSON endpoint worked and is cleaner to parse | orchestrator (scan, contemporaneous mode) | 2026-07-07_bill-crosscheck_P2.jsonl |
| 2026-07-07 | Built the LD-203 giving map (a P7 slice) as a spike to pressure-test the "who do they give money to?" half of the crypto Enterprise Map, and scoped it to LD-203 registrant-filed giving — explicitly NOT FEC | The story's money leg was confirmed to mean LD-203. Two scope calls were baked into the tool rather than left implicit: (1) attribution — LD-203 is filed by registrants, so the map is the giving of in-house registrants / trade groups / firms and is NOT attributable to an outside firm's clients (the tool flags a client-only query, e.g. a16z); (2) LD-203 ≠ FEC — crypto's headline election money (Fairshake Super PAC) is out of LD-203, so the tool says "disclosed LD-203 giving," never "total political spending." Whether to add an FEC join is a live editorial decision, logged, not taken | orchestrator (FEC scope RESOLVED 2026-07-07 — in scope; see FEC-scope row below) | (trace pending export) |
| 2026-07-07 | De-duplicated LD-203 amendments heuristically on the contribution identity tuple rather than trusting a raw item sum, and surfaced the raw figure alongside | The loader does not extract LD-203 `filing_type` (the raw record has it — "YY"/Year-End, mid-year, amendments; up to ~15 filings/registrant-year seen), so a raw sum double-counts amended reports. Absent the real filing_type, DISTINCT on (registrant+lobbyist+year+type+amount+payee+honoree+date+contributor) is the defensible middle path — same "never sum without deduping on the invariant" principle as the Senate `filing_period` rule. Flagged as a ranking signal to verify per-item; noted extracting LD-203 filing_type as a future loader enhancement | orchestrator | (trace pending export) |
| 2026-07-07 | Logged the crypto giving map as L029 `open` (tool demo), and deliberately separated its novel angle from the already-set-aside inaugural category | Consistent with the tooling lane (findings owned by teammates) and the L028 precedent. The $3M crypto-to-inaugural cluster overlaps the "corporate inaugural giving" category already set aside under Entities checked (2026-07-06), so it is not itself novel; the candidate-novel surface is the concentration of disclosed giving on the members who write crypto rules (Lummis, French Hill, Emmer, Toomey). Marked that the roster is name-guessed and needs P4 (free-text discovery) before the map can be called complete | orchestrator | (trace pending export) |
| 2026-07-07 | Kept entity-resolved matching as the tool default but added a `--loose` recall mode after the resolver split Payward/Kraken across three filer names | The precise join inherits lda-entity-resolver's entity boundaries, so a company whose name variants ("PAYWARD INC. D/B/A KRAKEN" vs "…FORMERLY KNOWN AS…") were split into separate entities is under-counted (~$1M of Kraken's inaugural giving missed). Rather than silently fuzzy-merge (the conflation risk CLAUDE.md warns against), the default stays conservative/auditable and `--loose` offers a direct-name-match recall check with a "eyeball for conflation" warning. Real fix is people/name resolution (cleanup C / P6) | orchestrator | (trace pending export) |
| 2026-07-07 | Built P4 (industry map) on a strict discovery-vs-serving split: FTS/BM25 + keyness only DISCOVER candidate terms; the cited layer is the deterministic keyword tagger (lobbying_issue_mentions) built from a versioned lexicon | Auditability is graded and CLAUDE.md forbids citing a black-box classifier — aggregate claims must cite exact SQL + sampled records. So the machinery that scales (search, log-odds, term mining) is quarantined in freetext_discovery.py, which proposes terms a human triages into industry_lexicon.json; nothing model-derived ever tags a finding. The FTS index is deliberately `stemmer='none'` so BM25 tokens match the raw vocabulary and it stays a discovery aid, not a second serving path | orchestrator | 2026-07-07_industry-map-P4_L030.jsonl |
| 2026-07-07 | Gave crypto its OWN facet tag (CRYPTO) rather than folding it into an ALI issue code, and made precision-first curation calls in the lexicon | Verified crypto scatters across 20+ ALI codes (FIN only 43.7%, then BAN/TAX/SCI/CPI/CDT/AGR…), so no single code IS the industry — the whole premise of mapping by vocabulary. Curation mirrored bill_aliases.json discipline: excluded ambiguous bare terms (token/mining/wallet/coin) and, after a spot-check, bare "clarity act" (it matched an unrelated athletic-training filing) — substituting the distinctive "digital asset market clarity"; kept distinctive bill-name phrases (GENIUS Act, FIT21). Version-stamped so a tagged set stays reproducible when the vocabulary grows | orchestrator | 2026-07-07_industry-map-P4_L030.jsonl |
| 2026-07-07 | Scoped the roster the map feeds to the money tools to CLIENT-side players only (the industry), excluding the outside registrant lobbying firms | ld203_giving on an outside firm (FS Vector, Invariant) reports THAT FIRM's giving, not the industry's — feeding them would conflate the lobbyists' money with the players' money. Client-side players are the companies + trade groups that ARE the industry (Coinbase, PayPal, Blockchain Association, Coin Center); crypto trade groups correctly land on the client side and stay in. Registrant firms are shown in the map for ecosystem context but kept out of the roster | orchestrator | 2026-07-07_industry-map-P4_L030.jsonl |
| 2026-07-07 | Logged the crypto player map as L030 `open` (tool demo) recall-first with human triage, keeping diversified giants (Visa/Mastercard/banks/Meta) IN the list but flagging incidental one-filing mentions (AARP/AFL-CIO) as triage-out | The named-actor rule: a finding names specific players with evidence, never "the whole list." The diversified giants legitimately lobby on crypto (their free-text names the vocabulary) and are the candidate-novel surface L029's name-guessed roster missed, so they stay; but a recall-first map with --min-docs 1 also pulls incidental mentions, which a finding-owner triages out (raise --min-docs). The map supersedes L029's hand-typed roster and feeds its giving leg; FEC enrichment (Fairshake Super-PAC money) now runs on this resolved roster | orchestrator | 2026-07-07_industry-map-P4_L030.jsonl |
| 2026-07-07 | Built the FEC enrichment (`fec_enrich.py`) as a bulk "pull the PAC, then match the roster" tool rather than one query per roster player | A crypto Super PAC has a bounded, itemized donor list (Fairshake's 2026 cycle = 53 receipts), so pulling every itemized receipt of each network committee once (a couple of pages/cycle) and matching the 535-player roster locally is far cheaper than 535 per-player queries, completes within even DEMO_KEY's shared-IP limit, and also surfaces network donors that weren't on the roster. Committee ids are resolved LIVE via `/committees` and filtered to Super PACs (a decoy non-qualified PAC "Protect Our Progress" C00768051 is correctly excluded), not trusted from memory | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Excluded FEC memo entries (`memo_code='X'`) from every total — caught by a published-number sanity check, not by the query running cleanly | The first run put Coinbase at $298.97M, which exceeds the entire network's reported haul — the classic openFEC Schedule A double-count: LLC-attribution memos are returned as separate rows whose amount is already in another line (a16z's $94.5M LLC gift is re-attributed to Andreessen/Horowitz as memos, manufacturing a phantom "individual giving" shadow exactly equal to the corporate line). Excluding them dropped Coinbase $298.97M→$166.47M — but that was still wrong (see the next-day line-11 row): the real fix layered on top of this. Same "never sum without excluding the double-count" lesson as the Senate `filing_period` dedup | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Attribute donor money from Schedule A LINE-11 contributions ONLY (excluding line-12 transfers, line-15/16 refunds, line-17 other receipts), and reconcile the sanity gate to FEC `contributions` NOT `receipts` — a correctness defect surfaced by an INDEPENDENT agent review of the linked FEC authorities (Rob's ask), not by the tool's own passing sanity check | The memo-fixed Coinbase figure ($166.47M) was still 56% too high. openFEC Schedule A files a crypto contribution on line 11 (the real gift, in-kind) and then, when the PAC LIQUIDATES the donated coin, re-files the sale proceeds on line 17 ("Other Federal receipts") as a fresh non-memo row (Coinbase's $59.9M "COINBASE COMMERCE (EXCHANGE)" rows = the same coins being sold, not a second donation). Summing line-11+line-17 double-counts the donor. The tool's own `--verify-totals` gate had CERTIFIED this as correct because it reconciled to FEC `receipts` (which includes those sale proceeds + $113M inter-committee transfers + interest) at <0.01% — false confidence. Restricting to line-11 and reconciling to FEC `contributions` gives the true Coinbase $106.59M and reconciles at <0.01% (Fairshake-2024 $217.42M≈$217.42M). One move also removes the $113M line-12 Fairshake→sister transfers and the interest/refund receipts. The independent authority review (agent over FEC's transaction-code + Schedule-A-line docs, IRE, ProPublica) was the thing that caught it — the brief's §3 checklist did not enumerate the in-kind→liquidation case; lesson logged that a passing internal reconciliation is not verification if it targets the wrong published field | Rob (asked for independent review), orchestrator (agent + fix) | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Classified FEC individuals by `entity_type=='IND'`, NOT by openFEC's `is_individual` boolean | openFEC flags many CORPORATE Super-PAC receipts `is_individual=True` (Ripple Labs Inc.'s $20M/$25M/$23M gifts come back `is_individual=True`, `entity_type='ORG'`). Trusting the flag misrouted a company's own treasury money into the personal-gift bucket and silently dropped the industry's single largest donor (Ripple) from the reconciliation — found only by asking "why is Ripple, the top Fairshake donor I saw in the raw data, missing from the output?". `entity_type` is the authoritative signal; genuine individual gifts (Coinbase execs, Wences Casares) stay in a separate section, never summed into corporate | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Kept FEC↔LDA name matches as flagged CANDIDATES (raw FEC name shown + confidence label), never a silent merge — and left a known false positive visible rather than hand-patching it | FEC contributor names don't align to LDA names, so the norm-key/token match (shared with `ld203_giving.py`) is a report to eyeball, consistent with the resolver's "ambiguity is a report, not a merge" and cleanup C / P6. The tool surfaces "THE GIVING BLOCK" (a crypto-philanthropy platform) matching roster "Block, Inc." as a `candidate` with the FEC name shown — a worked example of why a human triages the list; suppressing it by special-casing would hide the failure mode the label exists to expose. A finding names specific players with verified transaction ids, never "the whole list" | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Handled the api.data.gov key so it never enters the repo or a trace: env var (Rob set `DATA_GOV_API_KEY` via setx) read from the User scope at run time, with a gitignored `out/.fec_api_key` fallback; key stripped from every cached request and from `--json`; only the source LABEL is printed | The plan's hard constraint (key never committed/traced). Verified before hand-off: the key value does not appear anywhere under `out/`, no `api_key` field is stored in any cache file, and `out/` is gitignored so the cache + keyfile can't be committed. README §4 discloses the source + fetch date, not the key. The cache IS the evidence (FEC transaction_id + committee_id + endpoint + fetch date), the way scraped press text is primary and the live URL secondary | Rob (key), orchestrator (handling) | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Stress-tested the pulled Fairshake data against every FEC data trap in `reference/fec-campaign-finance.md` §3 before trusting the reconciliation, and added the caveats/guards it prescribes | The brief (Rob-provided) is the authoritative gotchas checklist. Audited each row against the real sample: IND/ORG split (✅ pull all of schedule_a, classify by entity_type), memo double-count (✅ excluded — and verified 0 roster players appear ONLY in memo rows, so unlike ActBlue conduits no donor identity is lost), amendments (✅ API reflects latest — 0 duplicate transaction_ids, reconciles <0.01% to published totals; added a defensive transaction_id DISTINCT for reuse), refunds/negatives (✅ none present; sum nets), $200 itemization floor (documented as a floor — negligible here since itemized ≈ published receipts), committee affiliation (✅ all 3 network committees resolved). The §3 checklist passed — but §3 did NOT enumerate the crypto in-kind→liquidation line-17 double-count, which a later independent agent review of the linked authorities caught (see the line-11 row above). So this stress-test was necessary but not sufficient; the brief is a floor, not a ceiling | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-07 | Logged the money-leg completion as L031 `open` (tool demo), reconciled against L029 (LD-203) and built on L030 (roster) | Consistent with the tooling lane (findings owned by teammates) and the L029/L030 precedent. The reportable surface is the delta — crypto's corporate-treasury Super-PAC money ($106.6M Coinbase, $96.5M Ripple, $94.5M a16z, all vs ~$0-$1.7M in LD-203) is invisible in the disclosed-lobbyist LD-203 view by law, so the industry money map understates political spend by ~60x for Coinbase (and unboundedly for Ripple/a16z, at $0 LD-203) until FEC is joined. The four legs now assemble a crypto Enterprise-Map finding: who (L030) / how-much-lobbying (P1) / LD-203 giving (L029) / Super-PAC money (L031). Scope stays "FEC-disclosed + LD-203-disclosed", never "total" (501(c)(4) + state money still out) | orchestrator | 2026-07-07_fec-enrichment_L031.jsonl |
| 2026-07-08 | Re-derived the crypto Enterprise Map FROM SCRATCH per Rob's instruction ("ignore any findings or lists we already have in the ledger and re-do"): serving table rebuilt from lexicon v1.0, roster regenerated, both money legs re-run — prior L029-L031 rosters/values not reused | Independent re-derivation is the QA the reset discipline exists for. Result: reproduces the L030/L029/L031 patterns from a cold start (508 players / 466 name-invisible; pure-play LD-203 $6.37M with the $5.0M inaugural cluster and Toomey/Emmer/Hill/Lummis concentration; FEC $106.59M/$96.5M/$94.5M) and surfaces one addition the old hand-typed roster missed (Galaxy Digital $1.08M LD-203). Convergence from scratch raises confidence; divergences would have been the story | Rob (scope), orchestrator (run) | (trace pending export) |
| 2026-07-08 | For the crypto package's "who they give to" headline, used a hand-triaged 105-name crypto-NATIVE roster (out/crypto_roster_pureplay.txt) instead of the full recall roster's LD-203 totals, and published all three tiers (full $238M / core $117M / pure-play $6.37M) side by side in the CSVs | LD-203 giving is organization-level and cannot be sliced to an issue: the recall roster's $238M total is dominated by incidental-mention giants (AARP alone dwarfs every pure-play; its $2.5M Capitol-tunnel donation and the CBC/CHC caucus giving belong to AARP/Realtors-class orgs, not the crypto lobby). Headlining that number would misattribute money to "crypto." The pure-play slice is the defensible "crypto-native giving" claim; the tiering is transparent in the package so reviewers can challenge the cut | orchestrator | (trace pending export) |
| 2026-07-08 | Computed healthcare pure-play share on ACTIVITY rows, not filings, after the filing-level metric read 100% for the U.S. Chamber | A self-filer submits ONE quarterly LD-2 listing dozens of issue codes, so "share of filings touching health" is ~100% for every diversified mega-filer — the U.S. Chamber (5.4% of activity rows health-coded) would have ranked as a health pure-play alongside PhRMA (67%). Caught by eyeballing the first players export; the corrected metric is what the package ships | orchestrator | (trace pending export) |
| 2026-07-08 | Published the three industry reviews as SHAREABLE PACKAGES under gitignored out/packages/ (dashboard HTML + CSV flat files + README with findings/caveats/QA-recipe + _build scripts + zips), all labeled "unverified research output for team review" | Rob's ask: deliverables teammates can review and QA without touching the raw corpus. Consistent with the output convention (generated artifacts → out/, never committed) and the tooling-lane discipline (findings owned by teammates): headline numbers are candidates with record anchors, not locked findings — L032/L033 hold the candidate angles. Dashboard display choices that editorialize are disclosed in-page: bubble size = crypto-tagged filings (all-issue spend would crown the U.S. Chamber a "crypto player"); known resolver-split families (Foris/Crypto.com, Payward/Kraken, a16z) combined in charts with per-variant rows kept in CSVs; AIPAC co-lobby framed "battlefield, not alliance" since disclosure data carries no direction-of-advocacy | Rob (ask), orchestrator (packages) | (trace pending export) |
| 2026-07-08 | Split every giving chart's recipients by GIVER TYPE (crypto-native vs diversified-core; health-focused vs mixed) and added party/state brackets to member recipients, per Rob's review ("was Toomey from crypto companies or from banks that also lobby on crypto?") — with the attribution caveat stated on the chart, not buried | Without the split, the "crypto giving" chart silently blended bank/card-network money into the crypto story (the diversified core's $110.7M dwarfs the natives' $6.37M). Two safeguards: (1) the split shows WHO funds each recipient, never WHY — a bank PAC's gift to a Financial Services member is not claimed as crypto-motivated; (2) party/state come from the corpus members table; members who left Congress in-window (Toomey, McHenry, Sherrod Brown, 19 total) are hand-mapped and every row carries a party_source column (members-table vs manual) so QA can audit the annotation. Person-name variants are merged on a matched-member key (nickname/initial-aware) because LD-203 recipient strings splinter ("Rep. French Hill" / "Rep. James French Hill" / "French Hill" were three rows); unmatched compound strings (joint-fundraising committees) stay raw rather than fuzzy-merged | Rob (ask), orchestrator (build) | (trace pending export) |
| 2026-07-08 | Answered a reviewer QA challenge (Rob: v2 ranked Toomey 2nd behind the inaugural; v3 ranks Emmer above him — "how are you QAing this?") by reconciling the change to raw records, and shipped a variant-audit CSV so every member merge is verifiable | The reorder is a CORRECTION, not drift: v2 charted raw recipient strings, so Emmer's giving splintered across five filed spellings and only the largest ("REP. THOMAS EARL EMMER JR", $50K) displayed; the member-keyed merge consolidates them to $76.4K. Chain verified against raw records: 15 LD-203 items honoring Emmer from crypto-native filers sum to $80,400 raw (a5822a04-964f-48dd-be4f-9f27f7d72388 Blockchain Assoc. 2022; c4e3b4ca-24cf-4b3a-b76f-95cd40959a4b Coinbase 2023; ae6bae3e-6d86-46a3-a64f-293a50b5af5e Galaxy 2025) − one identical doubled Galaxy pair ($3,500, tool amendment-dedup) = $76,900 − "Emmer for Congress" ($500, a committee, correctly NOT merged into the member) = $76,400 as charted. Toomey is a single item ($53,790, unchanged). Process gap acknowledged and fixed: the grain change (raw strings → member-keyed) shipped without a reconciliation artifact; packages now carry data/*_ld203_member_variant_audit.csv (every raw filed string behind every merged member row, 520 crypto / 491 healthcare rows), and the standing practice is archive-then-diff before regenerating a dashboard | Rob (challenge), orchestrator (reconciliation + audit) | (trace pending export) |
| 2026-07-09 | Moved P6 (person + political-committee resolver) to the FRONT of the build queue and EXPANDED its scope to map candidate-support committees — campaign committees, leadership PACs, joint-fundraising committees — to the member(s) they support, tier-labeled | Rob's call, driven by the industry-package review: (a) member name-splintering understated Emmer 35% until a QA challenge caught it (the ad-hoc member-keyed merge that fixed it lives in package build scripts, not a skill — hand-typed retiree + nickname lists included); (b) committee strings ("Emmer for Congress", "Lummis Victory Committee", JFC compounds) were left unmapped, so money supporting a candidate through indirect channels is invisible in member rollups. Scope rules set now: rollup-never-conflation (committees MAP with a tier label — direct and indirect never silently sum); JFC gifts attribute to all participants flagged unallocated (FEC transfer-based allocation = optional later); party committees and caucus institutions stay unmapped (their own recipients); sources = congress-legislators (members, party-per-term — new external dataset, dataset-primer first) + openFEC committee master (designations A/P/D/J). Prior order had P3 next; P6 also retroactively hardens the three shipped packages. Kickoff brief written in _Plan.md §9; stale "FEC ◀ NEXT" markers there corrected to BUILT while editing | Rob (scope + priority), orchestrator (brief) | (trace pending export) |
| 2026-07-07 | RESOLVED the open FEC-scope decision: FEC cross-reference is IN SCOPE as the crypto Enterprise-Map's next build; Rob obtained the api.data.gov key | Closes the two prior "FEC scope pending" flags (the LD-203-tool row and the L030 row above). Rationale: crypto's headline political money is the Fairshake Super-PAC network — an order of magnitude larger than the disclosed LD-203 giving and invisible in LD-203 by law — so "who they give to" is materially incomplete without it. It was correctly deferred until P4 produced a real entity-resolved roster (L030); pulling FEC for a name-guessed roster would have been wasted. Build plan (openFEC enrichment on L030's roster: money-in to the Super-PAC network, reconciled against ld203_giving, discrepancy = the reportable surface) is written as a cold-start kickoff brief in _Plan.md §9. Guards carried in: key read from an env var and NEVER committed/traced; README §4 discloses source + fetch date; scope stays "FEC-disclosed + LD-203-disclosed," never "total political spending" (dark money / state money still out) | Rob (scope + key), orchestrator (build plan) | 2026-07-07_industry-map-P4_L030.jsonl |
| 2026-07-09 | Built P6 from FEC BULK files (cm/ccl/cn per cycle) + ONE openFEC designation-D sweep, instead of the per-member openFEC crawl the kickoff brief sketched | The bulk candidate-committee linkage covers campaign committees AND the FEC-linked JFC participants with no API key and full reproducibility (cached zips); the one thing bulk lacks is leadership-PAC sponsor ids (ccl carries ~24 D rows/cycle vs ~850 D committees), so exactly that sweep runs against openFEC (cached pages, key env-only). JFCs with no candidate linkage (Lummis Victory Committee) get a CONSERVATIVE name-inference (unique first+last, else unique last name, whole-word) and every inferred row is flagged `inferred` — 642 of 742 JFC rows; participant lists may be partial and the resolver labels JFC money `shared, unallocated` accordingly | orchestrator | 2026-07-09_member-resolver-P6.jsonl (pending export) |
| 2026-07-09 | Accepted 6 explained number changes in the package retrofit as CORRECTIONS (regression otherwise byte-identical: 1,319/1,325 rows, 0 unexplained): (a) Sen. Robert Menendez's honoree items are no longer merged into his son Rep. Rob Menendez — titled strings re-attribute by chamber, untitled 'ROBERT MENENDEZ' reports as ambiguous and drops out of member merges; (b) 'REP DAN BISHOP' ($7,900, AIPAC) re-attributes from Sanford D. Bishop to Dan Bishop | Both were bugs in the 2026-07-08 ad-hoc matcher: rule_a's initial-expansion let 'DAN' match Sanford's middle initial 'D.', and the members pool lacked the departed Menendez senior and Dan Bishop entirely, so their strings fell into the only same-named member available. The new resolver returns ambiguity as a report (never merges), uses SEN./REP. titles to disambiguate chamber pairs (confidence `title-chamber`), and each correction is line-itemed in the packages' *_ld203_member_variant_audit_p6.csv. The pinned Emmer reconciliation ($76,400 direct + $500 campaign) reproduces exactly | orchestrator | 2026-07-09_member-resolver-P6.jsonl (pending export) |
| 2026-07-09 | Crypto dashboard revision (Rob's review): (a) subtitle cut to the plain one-liner; (b) the 'players with no crypto term in their name' stat tile REMOVED from the header row; (c) player-map bubbles re-sized to total lobbying DOLLARS (all issues) instead of crypto-tagged filing counts — reversing the 2026-07-08 display choice; (d) every filing behind the map is now linkable to its raw record: new data/crypto_player_filings.csv (5,179 filings x public lda.senate.gov URL, per-player counts reconcile with crypto_players.csv) + click-a-bubble filing list in the dashboard | Rob's calls on review. The dollar-size trap the old choice guarded against (an all-issue budget crowns incidental filers) is contained by keeping the map's SELECTION on sustained crypto-tagged filings and stating the all-issue caveat in the caption; the name-invisibility fact stays in the map footnote and README, just not as a KPI. Raw-filing links use the public lda.senate.gov filing pages (URL pattern verified against a988040b-8776-43bc-9311-58b95dcfda73, Coinbase/Checkmate 2026-Q1) so reviewers reach the primary record without the repo tooling; old dashboard archived to _archive/2026-07-09_v3-archived_filings-sized-bubbles.html; package re-zipped as crypto_package_2026-07-09.zip | Rob (asks), orchestrator (build) | 2026-07-09_member-resolver-P6.jsonl session (pending export) |
| 2026-07-10 | Crypto dashboard: added a per-widget QUERY-INFO layer (hover ⋯ → “View query info”) showing the actual SQL each widget's numbers came from; filings panel split LD-2 reports from LD-1 registrations with single-column vertical scrolling (and a chronology fix: filing_period was sorting alphabetically Q1→Q4→Q2→Q3) | Rob's debugging ask. Design choice: the SQL shown is EXTRACTED FROM THE EXPORT SCRIPTS' SOURCE at dashboard build time (export_crypto.py / export_player_filings.py / lda_ld203_giving.py's dedup CTE / resolve_entities.py's v_client_canonical_spend view), never hand-copied, so the displayed query cannot drift from the one that produced the CSVs; widgets whose numbers are NOT one SQL (LD-203 giving = tool run + member merge; FEC = openFEC API pull) show the pipeline + invocation instead of pretending to a query. Package re-zipped as crypto_package_2026-07-10.zip | Rob (ask), orchestrator (build) | 2026-07-09_member-resolver-P6.jsonl session continuation (pending export) |
| 2026-07-10 | Crypto dashboard: query-info now opens as a MODAL (backdrop/Esc/focus-return) instead of an inline panel, and the package gains DATA_DICTIONARY.md — every DB table/view the widget queries reference (9 relations, columns pulled live from the DuckDB schema with curated meanings) plus all 24 data/ CSVs column-by-column; each modal footer links to it | Rob's asks (modal + "is there a data dictionary?" — there wasn't: corpus-profile.md covers join keys/traps, not per-column meanings). Generator (_build/export_data_dictionary.py) reads the LIVE schema and CSV headers and flags any unannotated column rather than hiding it — 0 gaps at generation; trap notes (filing_period dedup, income-vs-expenses, registrant-scoped client_id, canonical-spend rule, LD-203 recipient semantics) are embedded on the columns they bite. Package re-zipped (crypto_package_2026-07-10.zip, now incl. the dictionary) | Rob (asks), orchestrator (build) | 2026-07-09_member-resolver-P6.jsonl session continuation (pending export) |
| 2026-07-10 | Crypto dashboard: EVERY widget is now click-through to its underlying records — trend quarters and press quarters (line-chart click), issue codes / spend bars / giving recipients / FEC players (bar click) each open a records panel with public links (lda.senate.gov filing + contribution pages, FEC receipts browser, original release URLs); five new indexes ship in data/ (trend_filings, issue_code_filings, spend_quarters, ld203_items, press_releases) | Rob's ask ("let the user see the actual underlying filings"). Correctness rule: each click-through list is built with ITS widget's exact semantics and reconciled at export time — trend lists use the chart's amendment-dedup/no-registrations CTE (counts match every quarter), giving items sum to every displayed row's chart figure, spend quarters sum to every bar, press counts match every quarter; the scatter panel states the blocks-vs-filings grain difference instead of hiding it. LD-203 items link via the verified /filings/public/contribution/<uuid>/print/ pattern; FEC rows link to the receipts browser pre-filtered (committee ids + contributor name) since FEC transactions aren't LDA records. Dashboard grows to ~1.6MB (all indexes embedded — accepted for a local team artifact); data dictionary regenerated, 0 unannotated columns | Rob (ask), orchestrator (build) | 2026-07-09_member-resolver-P6.jsonl session continuation (pending export) |
| 2026-07-10 | P3 turnover tracker built with two method locks: (a) terminations are DECLARED only — the senate T-family filing types `^[1-4](T|TY|@|@Y)$` — never inferred from absence-between-quarters; (b) engagement "newness" is grouped by registrant_id × resolved client ENTITY, never client_id. Candidate leads surfaced while validating the tool (the paid-pardon cluster) were ledgered as `open` (L034) for teammate triage, not investigated in-session | (a) absence-inference would fabricate exits from late posting and partial house dumps — the same class of trap as the H1c dedup fabrications; the T-family gives a filer-declared, citable event with the exact termination_date in the raw record. (b) client_id is re-issued on re-registration: the Checkmate/Gunvor 2025-Q4 pair showed the same engagement as both "terminated" and "new" in one quarter under client_id grouping. Lane discipline: this was a tooling session; L034 (Ver/Hatch/Schwartz pardon-seeking engagements, closed by termination filings) generalizes L021 and needs an outside-context-scan + editorial/legal-sensitivity review before anything more | orchestrator | 2026-07-10_p3-turnover-build.jsonl (pending export) |
| 2026-07-10 | Crypto dashboard: every filing row in the click-through panels (and the four filing-index CSVs) now carries `matched_keywords` — the exact curated lexicon phrase(s) that got the filing tagged | Rob's QA question on a live filing ("where is crypto called out in this filing?" — 4d712cd3-e92c-4dc9-9d3f-9b3eb7773a40, U.S. Chamber 2024-Q4: answer = ONE 'digital asset' mention inside the TAX activity, the IRS digital-asset broker-reporting rule) showed the tag-audit chain existed in the DB but wasn't surfaced: a reviewer opening the raw filing had no way to know WHICH phrase to look for. Now each row says e.g. 'matched: “stablecoin”' and the panel note tells reviewers to Ctrl-F the phrase on the linked page; full lists in the CSVs; dictionary updated (matched_keywords), 0 gaps | Rob (question), orchestrator (fix) | 2026-07-09_member-resolver-P6.jsonl session continuation (pending export) |
| 2026-07-10 | Built the P3 turnover review package (out/packages/turnover/: dashboard + 10 CSVs + README + DATA_DICTIONARY + zip) in the same visual/auditability frame as the three industry packages — house viz templates (_build/viz), per-widget "View query info" modals, click-through panels listing underlying filings with public lda.senate.gov links, table views, light/dark. Two build-integrity choices: (a) the export script produces every number by CALLING lda_turnover.py's own query functions and captures the executed SQL via a recording connection (the modal SQL cannot drift from what ran — stronger than the crypto build's regex extraction), with five reconciliation assertions (trend==tool counts; trend-top==tool ranking; displayed termination bars==their per-quarter audit rows; scoreboard counts==per-firm lists); (b) swap bars grouped one-per-client after the first render showed duplicate-looking bars (AARP ×2, Edison ×4 — one bar per term→hire pair), with every move kept in the click panel. Shared lib gained a backwards-compatible count/percent format for grouped bars (default "$" unchanged — crypto/AIPAC/healthcare pages unaffected until rebuilt). L034 (pardon-seeking cluster) shipped as a plain link-list card labeled candidate/unverified with the legal-sensitivity note. Verified: headless render light+dark clean; one filing link fetched live and cross-checked against the DB row (Stonington/TL Management 4T) | orchestrator | 2026-07-10_p3-turnover-build.jsonl session continuation (pending export) |
| 2026-07-10 | Built the pardons industry-map package (out/packages/pardons/: dashboard + 15 CSVs + README + zip) — new PARDONS lexicon facet (industry_lexicon.json v1.1) after whole-word precision triage (commute=transportation, amnesty=immigration, expungement=records-relief, compassionate-release=judicial-mechanism collisions all excluded and documented in display_only); the 54 mapped players hand-classed seeker / seeker_vehicle / advocacy / unclear in the export script's auditable CLASS dict; LD-203 giving leg shipped as CSVs but deliberately NOT charted (org-level, dominated by advocacy orgs' non-pardon activity — the largest "giving" items are one org's fundraising-gala honoree costs; charting would imply a pardon-attributability LD-203 cannot support); README states the recall boundary up front (Ver's "prosecution"-worded engagements are invisible to the vocabulary lens and belong to P3's turnover beat — the lenses are complements) | Rob's ask ("generate an industry map package for lobbying related to presidential pardons"). Conventions carried over from the crypto/turnover packages: matched_keywords on every filing row and click-through panel; click-through lists reconciled to chart figures at export time — the reconciliation check CAUGHT a real fan-out bug (entity_aliases carries one row per registrant-scoped senate_id, inflating the player-filings index 198→360 rows; fixed with a DISTINCT alias join) — plus declared-only termination, engagement dollars flagged non-issue-splittable, headless-render verification. CRYPTO serving-table regression verified byte-identical before/after the lexicon change | Rob (ask), orchestrator (build + class triage) | 2026-07-10_pardons-package_L035.jsonl (pending export) |
| 2026-07-11 | Turnover package extended to a MULTI-QUARTER beat report: 2026-Q1 exported and added to the one dashboard behind a quarter switcher (2025-Q4 | 2026-Q1; newest opens by default, #<quarter> URL hash deep-links; per-quarter KPIs/cards/caveats/queryInfo re-render on switch, trend + L034 card shared). Export now writes quarter-tagged CSV sets (turnover_<QTAG>_*.csv; corpus-wide trend files unsuffixed) so quarters accumulate; viz_build discovers every exported quarter. One correctness catch from the 2026-Q1 reconciliation gate: the churn-clients name join failed for Ballard because the firm renamed to 'BALLARD PARTNERS, LLC' — the scoreboard derives a firm's display name from the TARGET QUARTER's termination rows, and the export now mirrors that derivation exactly (all 8 reconciliation checks pass on both quarters). 2026-Q1 is labeled a floor throughout (newest quarter; terminations post with a lag; swap window reaches only backward). Q1 headline: the quarter's biggest terminated book is the L034 Schwartz/Burkman $960K pardon engagement; Hatch's $600K pardon engagement also terminated; new engagements 1,831 vs 2,590 in 2025-Q1. Verified: headless render of both quarter views + dark mode; package re-zipped (turnover_package_2026-07-11.zip) | orchestrator | 2026-07-11_turnover-q1-visual.jsonl (pending export) |
| 2026-07-11 | Fixed the healthcare package's activity-share bug (same `entity_aliases` fan-out class as the pardons-package fix) and republished the package rather than leaving the inflated numbers in an "unverified, team-QA" package | The pardons package's reconciliation checks caught the general trap on 2026-07-10 (naked per-row join to `entity_aliases` without a DISTINCT subquery fans out on the client/senate slice, which carries one row per registrant-scoped senate_id per raw_name); a full audit of every other package's alias joins on 2026-07-11 found exactly one more exposed site — the healthcare package's `acts` CTE (`out/packages/healthcare/_build/export_healthcare.py`), which computed activity-level health share with a naked `count(*)`/`sum()`. Fixed with the same house pattern (`LEFT JOIN (SELECT DISTINCT raw_name, entity_id FROM entity_aliases ...) ea`). Impact: PhRMA's `all_activities` was 21,398 instead of 887 (24x inflated), share 66.8%→68.8%; AHIP 74.7%→71.6%; AARP 21.2%→23.4%; Amazon 5.9%→5.2%; AHA 51.1% and Chamber 5.4% were unaffected. 9 players crossed the 50%/20% share-class boundary; 5 of those sat in the top-150 giving-map roster and were re-sorted between `healthcare_roster_focused.txt`/`_mixed.txt` (Cardinal Health/Takeda/EMD Serono → mixed; Illumina/Air Methods → focused; Bayer crossed 20% but stayed in mixed both ways, no roster move). Every other unaffected CSV was diffed against the pre-fix package (2026-07-08 zip) and reproduced byte-identical (or, for two CSVs whose queries have no deterministic tiebreaker, identical values in a different row order — confirmed non-issue). The LD-203 giving split was NOT re-pulled (its inputs come from a separate tool, `lda_ld203_giving.py`, unaffected by this bug) — `enhance_giving.py` was only repointed off a session-scratchpad path that could disappear, to the durable `out/packages/_build/inputs/` copies, and reproduced byte-identical. Dashboard rebuilt and headless-verified (light/dark); package re-zipped as `healthcare_package_2026-07-11.zip`; trap documented in `reference/corpus-profile.md` §4 so it's checked proactively on any new package instead of being rediscovered per-package | orchestrator | (trace pending export) |
| 2026-07-11 | Fixed the pardons engagement table's registration-only blind spot (Rob's QA catch: Zherka missing from the engagements widget) and adopted the conservative crediting rule: a quarter with NO activity text of its own is credited to a pardon engagement ONLY when the pair's LD-1 registration carries the tagged vocabulary, labeled `tag_basis='registration-only'`; quarters that declare other work are never re-attributed | Root cause was a real disclosure pattern, not a lexicon gap: some engagements state the pardon purpose only at registration (Zherka's $295K LD-2 carries no activity block at all; the text lives on RR 4907c947). The old query keyed taggedness to quarterly text, silently dropping such engagements and under-counting others whose no-text quarters held the dollars. Impact (all six added rows spot-checked vs raw records): market $4.37M→$6.21M, 21→27 engagements, 8→10 terminated; adds Zherka, Pramaggiore/RRB-Blagojevich, Schwartz/Merkava, Josh Smith/Caraway (Biden-era, terminated), Liddy, Diamond Street; corrects Daibes to his publicly-reported $1.00M. Pair grain makes the rule safe: (registrant_id, client_id) maps to one registration, so a no-text quarter's income cannot belong to a different engagement. Also: minimal compat shim in viz_build.py's crypto prep (`from_diversified_core` → fallback `from_diversified_forward`) because the split CSV moved to a three-way tier and was breaking ALL package rebuilds at import — flagged for the crypto session to rework properly | Rob (QA catch), orchestrator (fix) | 2026-07-11_pardons-context-scan.jsonl session continuation (pending export) |
| 2026-07-11 | Built the crypto INTENSITY REVISION per Rob's approval of the options memo (out/memo_crypto_intensity_options_2026-07-11.md): options A+B+D-gate as ONE package revision; the "crypto-attention dollars" model (option C) explicitly NOT built. (A) crypto ACTIVITY SHARE added to the players export — crypto-tagged senate activity blocks ÷ all the client's senate blocks, healthcare-parity semantics with the DISTINCT-alias fan-out guard; bands dedicated ≥25% (289 players) / engaged 5–25% (117) / ambient <5% (93) / n-a registrations-only (9); the Chamber worked example reproduces the memo exactly (19/555 blocks = 3.4%, band ambient; its C_TEC/CCMC arm 33.3%). (B) player map re-formed as a spend×share SCATTER (log-x all-issue canonical spend, y = share, dot size = crypto filings, dashed band rules at 5%/25%; new backwards-compatible scatterXY primitive in the shared viz lib); selection widened to top-60-by-filings ∪ core players with top-15 all-issue budgets, because the old filings-only cut had NEVER shown the ambient giants at all (Chamber rank 114 by filings, AARP 77 — this corrects the options memo, which wrongly assumed the Chamber dominated the old map; the real distortions were in the tiers, money list and giving split). (D-gate) diversified slice split at ≥5% activity share: crypto-forward 147 orgs / $71.87M vs ambient 15 / $38.85M (AARP $7.96M, U.S. Chamber $1.36M, Amazon, Meta, Wells Fargo…); ambient is NEVER drawn as a bar in a crypto-titled chart — it lives in the grouped-bar tooltip (new colorless context row), the click-through panel, the table views and the CSVs; the same gate applies to the money widget's diversified list; the FEC caption now states that all 12 nonzero Fairshake-network contributors are crypto-native (the only issue-attributable money on the page comes entirely from natives). A grey third-bar design was REJECTED on dataviz grounds: a sub-chroma-floor color cannot do series-identity work, and a bar would visually re-include exactly the money the gate excludes. Verification: enhance partition gate EXACT over 5,144 raw recipient strings; all widget-underlying reconciliations OK; p6 acceptance + smoke tests PASS; headless light+dark renders clean; per-CSV diff vs the archived pre-revision data fully classified (4 residual diffs = string_agg keyword-order nondeterminism ×3 + one LIMIT-60 boundary swap from the new deterministic tiebreakers). Built on the packager skill's viz templates (the same-day refactor); the pardons session's viz_build.py compat shim replaced with the real three-way rework; package re-zipped as crypto_package_2026-07-11.zip | Rob (decision: build A+B+D-gate, no attention-dollars model), orchestrator (build) | (trace pending export) |
| 2026-07-11 | Discovered and fixed a TOP-400 TRUNCATION BIAS in the 2026-07-08 giving-slice member merges, and republished the crypto giving numbers from EXHAUSTIVE runs (--top 999999): enhance_giving.py consumed only each lda_ld203_giving.py run's top-400 recipient rows, silently dropping sub-cutoff name variants from every member merge — the diversified pool's top-400 carried only $61.9M of its $110.72M. Regression gates on the re-cut: the native column is byte-stable on all 278 common display rows (the pure-play pool's 387 recipient rows were already exhaustive, so the pinned Emmer $76,400 direct + $500 campaign baseline is untouched — p6 acceptance PASS); forward+ambient equals the old diversified slice EXACTLY per raw recipient string; diversified display rows only ever GREW (193 rows, each itemized in the regenerated variant-audit CSV). Two shipped headline reads were truncation artifacts, corrected in the README: Toomey "all native, $0 diversified" → $53.8K native / $15K crypto-forward; Lummis "roughly even $45.5K/$56K" → $45.5K native / $123.5K crypto-forward + $43.5K ambient. Collateral hardening while re-running retrofit_p6 at the three-slice grain: member_resolve.py gained a curated FULL-STRING alias path (confidence='alias'; member_aliases.json v1.1 adds two sourced entries — 'LISA ROCHESTER'→B001303, 'APRIL MCLAIN DELANEY'→M001232 — spellings the token rules cannot reach because they drop or misspell a surname word), and retrofit's explained-correction rule now also accepts a full-name 'matched'/'alias' re-attribution as strictly-stronger evidence than the old matcher's single-initial fallback (surfacing two more old conflations: 'SEN CINDY HYDE SMITH' had merged into Christopher Smith, 'REP. JEFF JACKSON' into Jonathan Jackson); retrofit reruns with 0 unexplained changes across all three packages | orchestrator | (trace pending export) |
| 2026-07-11 | Built the industry-review-packager skill (PKG, _Plan.md §9 kickoff brief) as a spec-driven generator with a fixed invariant core and HARD reconciliation gates: trend==click-through per quarter, players==raw-filing index, press==release list, spend bars==quarter sums, giving by-year==deduped total — any mismatch aborts the build; the §4-safe DISTINCT `entity_aliases` join is written ONCE in the generator (the copy-paste of that join is what shipped the healthcare/pardons fan-out defects) | The five hand-built package pipelines had converged on one skeleton with ~80% duplicated SQL, and the duplicated join had already shipped two defects. Editorial content (KPI copy, caveats, per-widget card copy, class triage, lexicon facets) stays human-owned in the spec/lexicon files; the skill only assembles. Dashboards: new packages use a generic spec-copy-driven facet page; crypto/healthcare keep their bespoke pages via the legacy viz_build assembly the skill invokes (the brief's "stay per-package" option — one focused session, deadline Jul-15) | orchestrator | 2026-07-11_industry-review-packager_PKG.jsonl (pending export) |
| 2026-07-11 | Made every skill export DETERMINISTIC (ORDER BY tiebreakers everywhere; string_agg(... ORDER BY keyword); min() not any_value() for sample text) and strengthened the headless render gate to fail on any page JS error via an injected onerror probe, after a screenshot-size-only check passed a dashboard whose page JS had crashed right after the KPI row | Two failure classes found during the build: (a) legacy exports without tiebreakers reshuffle tie rows per run, which makes regression diffs noisy and had already produced boundary artifacts (7-way tie at 51 filings decided the last 3 healthcare top-150 roster slots; a LIMIT-60 registrant tie); a diffable quarterly-refresh regeneration needs byte-stable output. (b) The render check's first version certified a half-rendered page — 63KB of KPI tiles looks like a valid screenshot; the probe catches what size cannot | orchestrator | 2026-07-11_industry-review-packager_PKG.jsonl (pending export) |
| 2026-07-11 | CRITMIN lexicon facet (v1.2) precision calls: EXCLUDED bare 'battery' (wider EV/consumer-battery policy field, 4,223 filings + 35 artillery/assault-adjacent), 'lithium-ion'/'lithium ion' (dominated by the Setting Consumer Standards for Lithium-Ion Batteries Act consumer-fire-safety story, CSP 249), 'cobalt' (Cobalt-60 gamma-sterilization supply dominates the whole word, HCR/ENV/FOR), 'cathode' (2 filings), 'rare earth elements' (subsumed by whole-word 'rare earth'); INCLUDED 'lithium' (only 4 pharmaceutical-context filings of 1,315) and 'nickel' (only 4 coin-context filings of 255) after collision checks; copper/aluminum deliberately left untriaged for v1.2 | The facet exists to map the battery-MATERIALS supply-chain field around L024/BATT, not the wider battery-policy or EV story — precision-over-recall per the lexicon discipline; every exclusion reason recorded in display_only with counts. CRYPTO and PARDONS serving-table slices verified byte-identical (row count + content hash) before and after the v1.2 rebuild | orchestrator | 2026-07-11_industry-review-packager_PKG.jsonl (pending export) |
| 2026-07-11 | Corrected the PKG kickoff brief's assumption that 'BATTERY MATERIALS AND TECHNOLOGY COALITION' is a BATT name variant: it files through VENN STRATEGIES since 2022 ($2.21M reported), while BATT (Battery Advocacy for Technology Transformation) files through SMI + Cannon|Pearce from mid-2024 — two DISTINCT coalitions in the same space, kept as separate players and flagged not-to-conflate in the package README/caveats and L024 | The registrant and vintage evidence contradicts the same-org assumption (different firm, two years earlier); conflating them would fabricate a longer BATT history and a wrong spend total. The two-coalition fact is itself L024 evidence (the field held an undisclosed-membership battery-materials coalition before BATT emerged), as is the map's registrant table: SMI, BATT's own firm, is the FIELD's top outside registrant (223 tagged filings / 29 clients) — its client stable is the natural canvass for who else buys BATT's asks | orchestrator | 2026-07-11_industry-review-packager_PKG.jsonl (pending export) |
| 2026-07-11 | Ran the acceptance regressions against the COMMITTED baselines (git HEAD content), not the working tree, and deferred the crypto dashboard-assembly regression: a parallel session's crypto-intensity revision was mid-flight (uncommitted exporter/CSV/viz changes; it also committed d1b0c8c mid-build, whose Zherka registration-only engagement logic I ported into the skill so a future pardons regeneration cannot regress it) | The frozen-baseline contract in the brief means the committed state; comparing against a moving working tree would mix another session's in-progress revision into my verdicts. The healthcare regeneration exercised the same legacy viz_build dashboard path end-to-end (render-verified), so the deferred crypto dashboard run tests no unique code — the crypto spec's assembly will track whatever the intensity session lands. Passthrough files (giving-split/variant audits — enhance/retrofit-pipeline products, not skill outputs) are copied, not regenerated, and excluded from regression verdicts | orchestrator | 2026-07-11_industry-review-packager_PKG.jsonl (pending export) |
| 2026-07-13 | QA test of industry-review-packager (commit 224122c) on transportation: chose issue_codes lens over TRA, AVI, RRR, TRU, MAR, ROD; excluded the two other candidate codes on the task's own list, AUT (Automotive Industry) and AER (Aerospace) | Verified all 8 candidates exist in `senate_activities.general_issue_code` and against the challenge data manual's `lobbying_activity_issues.json` (the official ALI name list), then checked each code's actual top clients and a random sample of free-text before deciding: AUT's top clients are GM/Ford/Toyota/National Automobile Dealers Assoc./BorgWarner and its text is CAFE-standard/EV-mandate/NHTSA vehicle regulation — a manufacturing sector regulated alongside transportation, not itself the carriage of people/goods. AER's top clients are United Launch Alliance/Boeing/Airbus/SpaceX/RTX/Lockheed and its text is lunar programs, launch services, satellite/SAR imaging, spaceport licensing, export classification — the aerospace/defense-space industrial base, not transportation. By contrast MAR and ROD (the two codes with the most name-based ambiguity — "Marine/Maritime/Boating/Fisheries" and "Roads/Highway") were confirmed ON-TARGET by sampling their free-text: MAR text names the Merchant Marine Act, SHIPS for America Act, IMO Net-Zero Framework, port/HMT funding; ROD text names the Infrastructure Investment and Jobs Act, Surface Transportation Reauthorization, highway funding — both squarely transportation policy despite client rosters that include materials suppliers and insurers riding the code. The two most-cited bills in the resulting package (H.R.3684 IIJA, H.R.3935 FAA Reauthorization 2023) independently confirm the 6-code set is on target | orchestrator | 2026-07-13_industry-review-packager-qa_transportation.jsonl (pending export) |
| 2026-07-13 | Shipped the transportation package DATA-ONLY via `--skip-dashboard`, rather than extending the skill with a new generic issue_codes-lens dashboard assembly | SKILL.md documents two dashboard assemblies: the generic `facet` page (spec-driven, but its `assemble_facet()` reads CSVs — `{p}_player_filings.csv`, `{p}_trend_filings.csv`, `{p}_issue_code_scatter.csv` — that `run()` never produces for an `issue_codes`-lens package) and the legacy `viz_build` assembly (hardcoded per-package to crypto/healthcare, explicitly not to be silently routed through for a new package). Building a real third assembly (a generic issue_codes-lens page reusing facet_page.js/hc_page.js widgets, with its own reconciliation-safe data-assembly function and headless render verification) is substantial net-new development, and this session's brief was an explicitly bounded QA test with the Jul-15 submission deadline two days out. The data package (10 CSVs + roster + LD-203 giving + member rollup + zip) is complete, reconciliation-gated, and independently spot-checked; the gap is documented in the README and this row rather than silently worked around | orchestrator | 2026-07-13_industry-review-packager-qa_transportation.jsonl (pending export) |
| 2026-07-13 | Left `players.limit` unset for the transportation spec (unlike healthcare's `400`), after discovering that the 400-row cap combined with `activity_share`'s spend-descending sort silently excluded legitimate high-filings/low-spend pure-play players from `trans_players.csv` | Independently queried the top-60 transportation clients by tagged-filing count and diffed against the 400-row players export: 14 were missing, including genuine pure-play actors with modest all-issue budgets (Los Angeles County Metropolitan Transportation Authority, Miami-Dade County, Transportation Institute, National Association of Waterfront Employers) — precisely the "interesting axis" (pure-play vs side-desk) this package exists to show. crypto.json and critical-minerals.json ship their FULL player lists with no limit; healthcare's `400` was carried over from its pre-skill hand-built pipeline and was never checked against this failure mode (healthcare's own scale may hide the same latent gap — not investigated, out of scope, frozen package). Removing the limit for transportation (3,962 rows, in line with crypto/critmin precedent) is a spec-level fix, no code change; re-verified all four previously-missing players now appear with correct activity-share values (76.7%/32.9%/64.8%/50.4%) | orchestrator | 2026-07-13_industry-review-packager-qa_transportation.jsonl (pending export) |
| 2026-07-13 | Fixed `write_readme()` in `lda_package_industry.py` (shared skill code) so the generated "How to QA a number" section is conditional on whether a dashboard was actually built, instead of always claiming dashboard click-throughs and an embedded query-info viewer | Discovered while reading the transportation package's auto-generated README: with `--skip-dashboard`, the boilerplate still said "every chart is click-through" and "hover ⋯ → View query info" — both false for a data-only build, and this would misfire identically on any future `--skip-dashboard` package, not just this one. Added a `has_dashboard` check (`{id}_dashboard.html` exists) that swaps in accurate copy (cite `show_record.py` + the export functions' own SQL) when there is no dashboard. Verified no regression: `tests/pkg_acceptance.py`'s default run (critical-minerals integrity + CRYPTO/PARDONS lexicon guard) still passes — this function only emits README prose, never touches CSV/data generation | orchestrator | 2026-07-13_industry-review-packager-qa_transportation.jsonl (pending export) |
