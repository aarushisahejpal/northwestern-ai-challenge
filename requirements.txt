# Pinned versions — verified working 2026-07-04 on Python 3.12.10 (Windows).
# Evaluators: python -m venv .venv && pip install -r requirements.txt
duckdb==1.5.4
ijson==3.5.0
# spaCy is only needed for the press-text enrichment step (not the loader);
# pin when that script lands. Model: python -m spacy download en_core_web_sm
spacy
# Needed only by skills/finding-verifier/scripts/ocr_pdf.py, for outside-data
# verification against external PDFs (never the LDA/press corpus itself).
# Also requires the Tesseract OCR engine on PATH:
#   Windows: winget install --id UB-Mannheim.TesseractOCR
#   macOS:   brew install tesseract
#   Linux:   apt-get install tesseract-ocr
pymupdf==1.28.0
pytesseract==0.3.13
pillow==12.3.0
